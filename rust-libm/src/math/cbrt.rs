/* TESTS GENERATED BY CHATGPT */
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_cbrt_positive() {
        let result = cbrt(27.0);
        assert!((result - 3.0).abs() < 1e-6, "Cube root of 27.0 should be approximately 3.0");
    }

    #[test]
    fn test_cbrt_negative() {
        let result = cbrt(-8.0);
        assert!((result + 2.0).abs() < 1e-6, "Cube root of -8.0 should be approximately -2.0");
    }

    #[test]
    fn test_cbrt_zero() {
        let result = cbrt(0.0);
        assert_eq!(result, 0.0, "Cube root of 0.0 should be 0.0");
    }

    #[test]
    fn test_cbrt_small_value() {
        let result = cbrt(1e-6);
        assert!((result - 0.01).abs() < 1e-9, "Cube root of 1e-6 should be approximately 0.01");
    }

    #[test]
    fn test_cbrt_large_value() {
        let result = cbrt(1e9);
        assert!((result - 1000.0).abs() < 1e-3, "Cube root of 1e9 should be approximately 1000.0");
    }
}
